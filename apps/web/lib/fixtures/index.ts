/**
 * Transitional fixture exports for screens that still need runtime-backed data.
 *
 * Frontend follow-up should replace these imports with API-backed loaders page by page.
 */

export * from "../mock";